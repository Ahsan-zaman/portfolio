window.addEventListener("load",()=>
{
    $(".loadanimation").fadeOut("slow"),
    $("#tags ul li a").click(function(e){
        e.preventDefault()

    });
    const navBtn=document.querySelector("#navbtn"),
    nav=document.querySelector("nav")
    navLinks=document.querySelectorAll("nav a")
    navBtn.addEventListener("click",()=>{
        nav.classList.toggle('show')
    }),
    navLinks.forEach(a => {
        a.addEventListener('click',()=>{
            nav.classList.remove('show')
        })
    });
    $(window).scroll(function(){
        $(document).scrollTop()>=$(".progressbars")
        .offset().top-window.innerHeight/2&&$(".bar")
        .each(function(){$(this).find(".bar-inner")
        .css("width",$(this).attr("data-width"))});
    });
    anime({targets:"#wrapper .lines path",strokeDashoffset:[anime.setDashoffset,0],easing:"easeInOutSine",delay:function(el,i){return 1e3+100*i},duration:function(el,i){return 500+800*i},direction:"alternate",loop:!0})});